{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "telenieto",
      "runtimeExecutable": "python",
      "runtimeArgs": ["-c", "import os,http.server,socketserver; p=int(os.environ.get('PORT',8090)); socketserver.TCPServer(('',p),http.server.SimpleHTTPRequestHandler).serve_forever()"],
      "port": 8090
    }
  ]
}
